import { default as SelectableSearch } from './lib/variants/SelectableSearch';
import { default as fenrirSearch } from './lib/variants/fenrirSearch';
import { default as EarthShareSearch } from './lib/variants/EarthShareSearch';

const SearchField = {
  SelectableSearch,
  fenrirSearch,
  EarthShareSearch,
};

export default SearchField;
